Static file processor plugin for [Hexo](http://zespia.tw/hexo)
=================================

Simply copies any files from `source/_static/` directly into the `public/` directory during static file generation.
